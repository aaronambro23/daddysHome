---
description: BUILD POLICY — may also run the app or a dev server locally
---

Switch to BUILD POLICY: RUN for the rest of this session.

You may compile and test freely, and you may run the app or a dev server
locally when that is what actually verifies the change. Ask before a release
build or packaging step.

If I already have an instance running, say so before starting another rather
than competing with it for the port or the screen.

$ARGUMENTS
