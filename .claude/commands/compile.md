---
description: BUILD POLICY — compile and test, never run or serve
---

Switch to BUILD POLICY: COMPILE for the rest of this session.

You may compile, typecheck and run tests to prove the code builds. You may
not run, serve or launch anything — no `swift run`, no `bun dev`, no
`flutter run`, no localhost server, no opening a built `.app`. Ask before a
release build or packaging step.

I run the app myself so I can watch the live logs. When a change needs that to
be verified, say so and give me the command.

$ARGUMENTS
